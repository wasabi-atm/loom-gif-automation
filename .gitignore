# Secrets — never commit
.env
.env.local
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Generated media & run artifacts
output/
!output/.gitkeep
.cache/
*.log

# Face cam takes ARE tracked, so the rotation travels with the repo and a clone
# can render immediately. GitHub warns above 50MB per file and rejects above
# 100MB — trim exports, or move to Git LFS if they get big.

# macOS
.DS_Store
